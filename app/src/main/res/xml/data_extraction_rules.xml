<?xml version="1.0" encoding="utf-8"?>
<!--
  Guardrail only. android:allowBackup="false" in the manifest is what actually turns off
  Android Auto Backup; these rules keep cloud upload off if it is ever switched back on,
  while still allowing a local transfer to a new phone. Backups go through Settings.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
        <exclude domain="external" />
    </cloud-backup>
    <device-transfer>
        <include domain="database" path="." />
    </device-transfer>
</data-extraction-rules>
